# Docker Compose: Nginx + Python + PostgreSQL
Этот проект разворачивает многоконтейнерное веб-приложение с использованием Docker Compose.
## Архитектура проекта

Проект состоит из трёх сервисов:

### 🐍 Python Application
- **Контейнер:** `my_python_app`
- **Сборка:** из директории `./python_app`
- **Назначение:** основное приложение, генерирующее HTML контент
- **Зависимости:** зависит от PostgreSQL
- **Перезапуск:** при ошибках

### 🗄️ PostgreSQL Database
- **Контейнер:** `my_postgres`
- **Образ:** `postgres:17.7`
- **База данных:** `analytics_db`
- **Пользователь:** `user` / `password`
- **Порт:** 5432 (внутренний) → 7777 (внешний)
- **Данные:** сохраняются в volume `pg_data`

### 🌐 Nginx Web Server
- **Контейнер:** `my_nginx`
- **Образ:** `nginx:latest`
- **Порт:** 80 (внутренний) → 8080 (внешний)
- **Конфигурация:** использует кастомный `nginx.conf`
- **Статика:** обслуживает HTML из volume `html_data`
- **Зависимости:** зависит от Python приложения

## Требования

- Docker
- Docker Compose

## Быстрый старт
1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd <project-directory>
```

2. Запустите приложение:
```bash
docker-compose up -d
```
3. Откройте в браузере:
```
http://localhost:8080
```

## Доступ к сервисам

| Сервис | URL | Порт | Назначение |
|--------|-----|------|------------|
| Nginx | http://localhost:8080 | 8080 | Веб-сервер |
| PostgreSQL | localhost:7777 | 7777 | База данных |

## Структура данных

- **PostgreSQL данные:** сохраняются в Docker volume `pg_data`
- **HTML контент:** общий volume `html_data` между Python приложением и Nginx
- **Конфигурация Nginx:** монтируется из `./nginx.conf`

## Сеть

Все контейнеры подключены к bridge-сети `my_net`, что позволяет им общаться между собой по именам сервисов.

## Переменные окружения PostgreSQL

- `POSTGRES_USER=user`
- `POSTGRES_PASSWORD=password`
- `POSTGRES_DB=analytics_db`

## Примечания

- PostgreSQL доступен на порту 7777 для внешних подключений
- Python приложение генерирует HTML в общий volume, который затем обслуживается Nginx
- Все сервисы настроены на автоматический перезапуск при различных условиях