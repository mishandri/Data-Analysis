# Анализ нарушений условий платёжной системы

Аналитический запрос для выявления пользователей, чьи платежные операции нарушают условия платёжной системы по ограничению суммы платежей за временной интервал.

## 📋 Условие нарушения

Платёжная система устанавливает ограничение:  
**Сумма успешных платежей одного пользователя за 24 часа не должна превышать 20 000 000 единиц.**

## 🗂️ Структура данных

### Исходная таблица `payment`
Содержит информацию о платежных операциях:

| Поле | Тип | Описание |
|------|-----|----------|
| `account_id` | UInt64 | Идентификатор пользователя |
| `operation_id` | UInt64 | Идентификатор операции |
| `amount` | Decimal | Сумма платежа |
| `operation_datetime` | DateTime | Дата и время операции |
| `status` | String | Статус операции |

## 📊 Логика анализа

Запрос выполняет скользящее агрегирование для каждого пользователя, вычисляя:
1. **Сумму платежей** за 24 часа, предшествующих каждой операции
2. **Количество операций** за тот же период

### Алгоритм работы:
1. Фильтрация только успешных платежей (`status = 'completed'`)
2. Для каждой операции определяются границы 24-часового окна:
   - `interval_start`: текущее время операции - 24 часа
   - `interval_end`: текущее время операции
3. Вычисление скользящей суммы и количества операций в этом окне
4. Выбор операций, где накопленная сумма превышает пороговое значение

## 🔧 Настраиваемые параметры

В начале запроса можно настроить параметры анализа:

```sql
WITH 
24 AS hours,                       -- Временное окно в часах
20_000_000 AS threshhold,          -- Пороговая сумма
-- или явное указание типа:
-- toUInt64(20_000_000) AS threshhold
```
## 📈 Выходные данные
Результирующий набор содержит:

| Поле | Описание |
| ------ | ----- |
| account_id | Идентификатор пользователя |
| amount | Сумма текущей операции |
| interval_start | Начало 24-часового интервала |
| interval_end | Конец интервала (время операции) |
| interval_amount | Накопленная сумма за интервал |
| interval_count | Количество операций за интервал |

*Примечание: Выводятся все операции, на момент которых было зафиксировано превышение порога.*

## ⚡ Особенности реализации

### Оптимизация производительности:

Используется оконная функция с RANGE, что эффективно для временных рядов

Предварительная фильтрация по статусу сокращает объем обработки

### Точность временных окон:

Интервал рассчитывается в секундах для соответствия формату DateTime

RANGE BETWEEN hours*60*60 PRECEDING корректно работает с timestamp-ами

### Гибкость:
- Легко изменить временное окно или пороговое значение
- Можно добавить дополнительные условия фильтрации

## 📝 Примечания

Запрос написан для ClickHouse с учетом его особенностей синтаксиса

**Для больших объемов данных рекомендуется:**

Добавить индексы на account_id, operation_datetime

Рассмотреть материализованные представления для предрасчета

При изменении порога на очень большое число может потребоваться коррекция типа данных для threshhold

## 🎯 Цель анализа
Выявление потенциально мошеннических операций, технических ошибок или пользователей, требующих дополнительной верификации согласно требованиям платёжной системы.